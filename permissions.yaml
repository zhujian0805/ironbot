tools:
  - priority: 100
    name: ".*"
    desc: "Allow all tools while permissions are being simplified"
mcps:
  - priority: 100
    name: ".*"
    desc: "Allow all MCPs until scoped rules are introduced"
commands:
  - priority: 100
    name: ".*"
    desc: "Allow every command while we stabilize policy guarding"
skills:
  - priority: 100
    name: ".*"
    desc: "Enable every skill by default for the simplified policy"
resurces:
  - priority: 100
    name: ".*"
    desc: "Permit all resource paths until we define more specific rules"
